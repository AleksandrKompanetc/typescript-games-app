function Game({ name, description, version}) {
  return (
    <div>
      <p>Name: {name}</p>
      <p>Description: {description}</p>
      <p>Version: {version}</p>
    </div>
  )
}

export default Game;